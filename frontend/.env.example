# ── Copy this to .env.local for local development ────────────────────────────

# Backend API URL
# Local dev: leave as /api (Vite proxy handles it → localhost:8000)
# Production: set to your full Render URL, e.g:
#   VITE_API_URL=https://fintrack-api.onrender.com
VITE_API_URL=/api
